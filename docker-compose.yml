# prod: cashback_bot
services:
  cashback_bot:
    build: .
    image: ${CASHBACK_BOT_IMAGE:-cashback_bot:local}
    container_name: cashback_bot
    command: ["/app/.venv/bin/python", "run.py"]
    env_file:
      - .env        
    volumes:
      - ./logs:/app/logs
    restart: always
